<%-include("template/head",{Title:"Detalles"})%>
    <header>
        <div class="container">
            <h1 class="text-center text-danger">Detalle del empleado</h1>
        </div>
        <main class="container">
            <% if (error) { %>
                <h2 class="text-center">
                    <%=mensaje%>
                </h2>
                <a href="/plagas" class="btn btn-primary btn-block">Volver a la tabla de
                    <% } %>
                        <% if (!error) { %>
                            <table class="table table-hover border">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Empresa</th>
                                        <th>Relacion</th>
                                        <th>Vigencia</th>
                                        <th>Correo</th>
                                        <th>Acción</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <tr>
                                        <td>
                                            <%=auspiciador.id%>
                                        </td>
                                        <td>
                                            <%=auspiciador.empresa%>
                                        </td>
                                        <td>
                                            <%=auspiciador.relacion%>
                                        </td>
                                        <td>
                                            <%=auspiciador.vigencia%>
                                        </td>
                                        <td>
                                            <%=auspiciador.correo%>
                                        </td>
                                        <td><button class="btn btn-danger btn-sm" id="btn_eliminar-auspiciador"
                                                data-id="<%=auspiciador.id%>">Eliminar</button></td>
                                    </tr>
                                </tbody>
                            </table>
                            <% } %>
        </main>
        <script src="/Js-eliminar/auspiciador-eliminar.js"></script>

        <%-include("template/footer")%>