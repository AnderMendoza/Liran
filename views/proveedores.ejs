<%-include("template/head",{Title:"Proveedores"})%>

<header class="container">
  <h1 class="display-2 text-primary text-center mb-3">Proveedores</h1>
  <p>En esta tabla podemos ver una empresa o una persona fisica que  proporciona bienes o servicio a esta empresa.</p>
  <div class="container">
    <a class="btn btn-warning btn-block my-4" href="/proveedores/registrar">Agregar proveedores</a>
  <table class="table table-striped">
    <thead>
      <tr>
        <th>ID</t>
        <th>Empresa</th> 
        <th>Correo</th>
        <th>Acción</th>
      </tr>
    </thead>
    <tbody>
        <% if (arrayproveedores.length>0) { %>
            <% arrayproveedores.forEach(proveedores => { %>
               <tr>
                   <td><%=proveedores.id%></td>
                   <td><%=proveedores.empresa%></td>
                   <td><%=proveedores.correo%></td>
                    <td><a href="proveedores/<%=proveedores.id%>" class="btn btn-warning btn-sm">Editar</a></td>
               </tr>
            <% }) %>
           <% } %>
    </tbody>
  </table>
</div>


<%-include("template/footer")%>