<%-include("template/head",{Title:"Empleados"})%>
<header>
    <div class="container">
    <h1 class="text-center text-danger">Detalle del empleado</h1>
    </div>
    <main class="container">
        <% if (error) { %>
         <h2 class="text-center"><%=mensaje%></h2>
         <a href="/plagas" class="btn btn-primary btn-block">Volver a la tabla de 
        <% } %>
        <% if (!error) { %>
            <table class="table table-hover border">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Nombre</th>
                        <th>Apellido</th>
                        <th>Email</th>
                        <th>Edad</th>
                        <th>Acción</th>
                    </tr>
                </thead>
                <tbody>
                        <tr>
                            <td><%=empleadoarray.id%></td>
                            <td><%=empleadoarray.nombre%></td>
                            <td><%=empleadoarray.apellido%></td>
                            <td><%=empleadoarray.correo%></td>
                            <td><%=empleadoarray.edad%></td>
                            <td><button class="btn btn-danger btn-sm" id="btn_eliminar-empleado"
                            data-id="<%=empleadoarray.id%>">Eliminar</button></td>
                        </tr>
                </tbody>
            </table>
        <% } %>
    </main>
<script src="/Js-eliminar/empleado-eliminar.js"></script>

<%-include("template/footer")%>