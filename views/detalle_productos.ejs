<%-include("template/head",{Title:"Detalles"})%>
    <header>
        <div class="container">
            <h1 class="text-center text-danger">Detalle del Producto</h1>
        </div>
        <main class="container">
            <% if (error) { %>
                <h2 class="text-center">
                    <%=mensaje%>
                </h2>
                <a href="/productos" class="btn btn-primary btn-block">Volver a la tabla de
                    <% } %>
                        <% if (!error) { %>

        <form id="formularioEditarPLaga" data-id="<%=producto.id%>">
            <input type="text" class="form-control my-2" name="nombre" value="<%=producto.nombre%>" id="nombreProducto">
            <input type="text" class="form-control my-2" name="marca" value="<%=producto.marca%>" id="nombreMarca">
            <input type="text" class="form-control my-2" name="precio" value=" <%=producto.precio%>" id="precioCantidad">
            <button class="btn btn-warning btn-block" type="submit">Editar</button>
        </form>
        <hr>
        <button class="btn btn-danger btn-sm " id="btn_eliminar-producto"
        data-id="<%=producto.id%>">Eliminar</button>

        <% } %>
        </main>
        <script src="/Js-eliminar/producto-eliminar.js"></script>

        <%-include("template/footer")%>