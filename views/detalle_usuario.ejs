<%-include("template/head",{Title:"Detalles"})%>
<header>
    <div class="container">
    <h1 class="text-center text-danger">Detalle del usuario</h1>
    </div>
    <main class="container">
        <% if (error) { %>
         <h2 class="text-center"><%=mensaje%></h2>
         <a href="/usuarios" class="btn btn-primary btn-block">Volver a la tabla de 
        <% } %>
        <% if (!error) { %>
            <table class="table table-hover border">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Nombre</th>
                        <th>Apellido</th>
                        <th>Email</th>
                        <th>Acción</th>
                    </tr>
                </thead>
                <tbody>
                        <tr>
                            <td><%=usuario.id%></td>
                            <td><%=usuario.nombre%></td>
                            <td><%=usuario.apellido%></td>
                            <td><%=usuario.correo%></td>
                            <td><button class="btn btn-danger btn-sm" id="btn_eliminar"
                            data-id="<%=usuario.id%>">Eliminar</button></td>
                        </tr>
                </tbody>
            </table>
        <% } %>
    </main>
<script src="/Js-eliminar/main-eliminar.js"></script>

<%-include("template/footer")%>
